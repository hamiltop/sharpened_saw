defmodule Ex3 do
  @moduledoc """
    Learn to use the Enum module and the Stream module
  """

  @doc """
    The requirements of FizzBuzz are to produce a list of the numbers 1 to 100. If the number is divisible by 3,
    replace the number with "Fizz". If the number is divisible by 5, replace the number with "Buzz". If it is 
    divisible by 3 and 5, replace the number with "FizzBuzz".

    This can be done a number of ways, I recommend looking at the Stream module
  """
  def fizzbuzz do
  end
 
  @doc """
    Accepts a list and returns a new list that meets the following characteristics:

    * For every odd element of the original list, the new list contains the square of that element
    * Include every element of the original list that is divisible by 4.
    * Discard all other elements
  """ 
  def scrambled_eggs(list) do
  end
end
